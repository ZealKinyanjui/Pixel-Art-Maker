// Select color input
const chooseColor = ;


// Select size input
let gridWidth, gridHeight, color;

// When size is submitted by the user, call makeGrid()

function makeGrid() {

  for (var row = 1; row<=gridWidth; row++) {
    document.
    for (var col = 1; col<=gridLength; col++) {
      document.
  }
}

  do (document.clear(*function*) {
    while ( = ) 
      ++;}

